spring:
  application:
    name: HRManagementSystem

  datasource:
    url: jdbc:postgresql://localhost:5432/mypostgres
    username: arzu
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update   # "create", "create-drop", "update", "validate" kimi dəyişə bilərsən
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080